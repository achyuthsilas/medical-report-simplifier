# Optional: set this in production to point at your deployed FastAPI backend.
# In dev, leave it empty — Vite proxy in vite.config.js handles /api → http://localhost:8000

# VITE_API_URL=https://your-backend.onrender.com
